import SingleStudio from '~/lib/pages/SingleStudio';

const page = () => {
  const data = {}; // Replace with the actual data object
  const services = {}; // Replace with the actual services object

  return <SingleStudio data={data} services={services} />;
};

export default page;
